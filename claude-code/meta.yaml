name: Claude Code
repo: anthropics/claude-code
stars: 109558
forks: 18175
language:
  - TypeScript
loc: 510000
license: Proprietary
created: "2025-02"
data_as_of: "2026-04"

category: coding-agent
maturity: production
deployment: npm

characteristics:
  architecture: "single-file agentic loop (query.ts, 1729 lines), 40+ tools as pure functions via buildTool() factory, streaming tool execution with RWLock"
  code_organization: "510K LOC TypeScript, 1903 files, Bun runtime with compile-time feature flag macros, Zod v4 schemas throughout"
  security_approach: "per-tool permission model with trust level scoping, seatbelt sandbox for process execution"
  context_strategy: "4-layer cascade: compact conversations losslessly, summarize tool outputs, truncate old turns, drop least-relevant context"
  documentation: "internal docs cover feature flags, tool contracts, context cascade; inline Zod schemas serve as living API docs"

features:
  sandbox: seatbelt
  agent_loop: custom
  context_management: 4_layer_cascade
  stuck_detection: false
  multi_agent: true
  mcp_support: true
  plugin_system: false
  security:
    layers: 1
    types: [per-tool-permission, sandbox-exec]
  providers:
    count: 1
    names: [anthropic]

key_finding: "4-layer context cascade, hidden pet system (18 species), 1729-line god object"

worth_stealing:
  - "4-layer context cascade: lossless before lossy, local before global — progressive degradation preserves maximum information"
  - "Streaming tool execution with RWLock: start executing read-only tools while the model is still generating"
  - "buildTool() factory over inheritance: each tool is a plain object with schema, permissions, execution, UI rendering co-located"

easter_eggs:
  - "18 virtual pet species with hex-encoded names (duck through chonk), RPG stats, 5 rarity tiers, 1% shiny variants"
  - "KAIROS autonomous mode — Greek for 'the right moment', enables fully autonomous operation"
  - "Anti-distillation fake tools that poison training data if competitors try to distill behavior"
  - "tengu_ prefix on all runtime feature gates — Japanese for heavenly dog, the project codename"

tags:
  - coding-agent
  - cli
  - context-management
  - streaming-tools
